[package]
name = "fquery"
version = "1.0.0"
edition = "2024"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

[dependencies]
rayon = "1.10"
glob = "0.3"
thiserror = "2"
clap = { version = "4", features = ["derive"] }
regex = "1"
